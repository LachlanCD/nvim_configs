# nvim_configs
## For Ubuntu
```bash
sudo apt-get update
sudo apt-get upgrade
sudo add-apt-repository ppa:neovim-ppa/unstable
sudo apt install neovim npm gcc ripgrep
```
list of releases while v11 is cooked https://github.com/neovim/neovim/releases
